# Build artifacts
/bin/
/dist/
*.exe

# Per-user / runtime state (not source)
*.sqlite
*.sqlite-shm
*.sqlite-wal
ccdash-server.log

# Editor / tooling
.idea/
.vscode/
*.swp
*.swo

# Claude Code's per-project local settings (kept out of source for safety —
# see docs/research.md §5 for the threat model).
.claude/settings.local.json
